[project]
config_version = 1

[package]
name = "@unsareport/define"
version = "0.1.1"
displayName = "Report Variable Define & Export"
description = "Minimal utilities for exporting metadata variables from Typst documents"
tags = ["metadata", "utilities"]
command_prefix = "define"

[components]
files = ["lib.typ", "scripts/**/*"]

[templates]
files = []
